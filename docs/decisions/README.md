# Decision records — moved

Architecture Decision Records now live in [`docs/adr/`](../adr/) as sequential
`NNNN-slug.md` files. See [`docs/adr/README.md`](../adr/README.md) for the index.

This directory is retained only for history; do not add new records here.
